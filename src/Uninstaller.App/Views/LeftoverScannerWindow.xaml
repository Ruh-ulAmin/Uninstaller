<Window x:Class="Uninstaller.App.Views.LeftoverScannerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Leftover Scanner" Height="560" Width="760" WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4"
                   Text="System-wide leftover scan" />
        <TextBlock Grid.Row="1" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,10" TextWrapping="Wrap"
                   Text="This looks for orphaned uninstall registry entries and folders under Program Files / ProgramData / AppData that no longer belong to any installed program. Matching is heuristic - review carefully before deleting anything. Nothing is pre-selected." />

        <Grid Grid.Row="2">
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                <ListView x:Name="ItemsList" BorderThickness="0">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="34">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Type" Width="90" DisplayMemberBinding="{Binding KindDisplay}" />
                            <GridViewColumn Header="Location" Width="420" DisplayMemberBinding="{Binding Item.Path}" />
                            <GridViewColumn Header="Size" Width="80" DisplayMemberBinding="{Binding Item.SizeDisplay}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>

            <Border Background="{DynamicResource PanelBackgroundBrush}" x:Name="ScanningOverlay">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="6" Margin="0,0,0,10" />
                    <TextBlock Text="Scanning for leftovers..." Foreground="{DynamicResource TextSecondaryBrush}" />
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Rescan" Width="100" Margin="0,0,8,0" Click="RescanButton_Click" />
            <Button Content="Close" Width="100" Margin="0,0,8,0" Click="CloseButton_Click" />
            <Button Content="Remove Selected" Width="140" Style="{StaticResource DangerButton}"
                    Click="RemoveButton_Click" />
        </StackPanel>
    </Grid>
</Window>

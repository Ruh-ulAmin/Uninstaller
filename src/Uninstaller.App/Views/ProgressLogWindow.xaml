<Window x:Class="Uninstaller.App.Views.ProgressLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Uninstaller" Height="420" Width="620" WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10" />

        <ProgressBar x:Name="ProgressIndicator" Grid.Row="1" Height="6" IsIndeterminate="True" Margin="0,0,0,10" />

        <Border Grid.Row="2" Background="{DynamicResource PanelAltBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
            <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto" Padding="10">
                <ItemsControl x:Name="LogItems">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12"
                                       Margin="0,1" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Width="100" Margin="0,0,8,0" Click="CancelButton_Click" />
            <Button x:Name="CloseButton" Content="Close" Width="100" Style="{StaticResource AccentButton}"
                    IsEnabled="False" Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>
